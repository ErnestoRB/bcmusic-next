import path from "path";

export const FONTS_PATH = path.join(process.cwd(), ".runtime", "fonts");
export const BANNERS_PATH = path.join(process.cwd(), ".runtime", "banners");
export const ROUTES_PATH = path.join(process.cwd(), ".runtime", "routes");
